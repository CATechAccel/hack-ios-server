version: "3.8"

services:
  server:
    build:
      context: .
      # 本番環境用の Dockerfile を指定
      # server.dockerfile は開発環境のみ想定
      dockerfile: Dockerfile
      target: builder
    depends_on:
      - mysql
    image: hack-ios-server-image
    container_name: hack-ios-server-cnt
    command: air -c ./config/.air.toml
    ports:
      - 8080:8080
    volumes:
      - .:/app
    environment:
      MYSQL_USER: root
      MYSQL_ROOT_PASSWORD: "hack-ios-server"
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_DATABASE: hack_ios_server_api

  mysql:
    image: mysql:5.7
    ports:
      - 3306:3306
    volumes:
      - ./db/init:/docker-entrypoint-initdb.d
      - db-data:/var/lib/mysql
      - ./db/my.cnf:/etc/mysql/conf.d/my.cnf
    environment:
      MYSQL_ROOT_PASSWORD: "hack-ios-server"

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    environment:
      PMA_ARBITRARY: 1
      PMA_HOST: mysql
      PMA_USER: root
      PMA_PASSWORD: "hack-ios-server"
    links:
      - mysql
    ports:
      - 4000:80

  swagger-ui:
    image: swaggerapi/swagger-ui:latest
    environment:
      SWAGGER_JSON: /api/api-document.yml
    volumes:
      - ./api-document.yml:/api/api-document.yml:ro
    ports:
      - 3000:8080
volumes:
  db-data:
    driver: local